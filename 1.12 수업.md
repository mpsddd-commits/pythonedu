# Python 파일 *.py
# jupyter Notebook *.ipynb 이거 언제 쓰는지 다시 알아보자
# compile언어vs 인터프리터언어
# 객체지향 , 인터프리터언어, 동적타이핑언어 다시 한번 검색해 보고 알아보기
# 파이썬은 모든 운영체제 이용 가능 비슷한 예시로 자바가 있다.
# 설치 프로그램 - 파이썬, 아나콘다(이건 상업으로는 유료사용이라 사용하지 않는것 추천)
# 코딩 프로그램 - 쥬피터, vs코드
# 판다스는 ai에이전트에 필요하기 때문에 반드시 진입해야한다.
# 뷰티풀숲은 자료수집용 vs 셀레니움(이건 자동 자료수집 -ex 매크로)
# 스트림릿 -빠르게 화면표현 프로그램 - html,자바스크립트,css 몰라도 사용 가능한 프로그램

# python study01.py(파일명.py) -실행하는 방법 1 -cmd에서 +파일 위에 재생버튼 누르면 python확장명으로 나옴 - 실행하는 방법2

# 터미널에서 pip list 누르면 개인pc에 설치된 파일들이 보인다. => 설치가 되어있으니 어디서나 쓸수 있고 라이브러리를 개인화가능하다. 어디서나 설치 가능하다. 글로벌?? like 

# uv를 이용하여 프로젝트를 만들면 => 파이썬 실행 속도가 정말 빠름 인터넷에 uv python치면 깃허브 나오는데 거기 오른쪽에 파일 이동하면 uv에 대한 설치 와 설명이 있다. ==> 설치 따로 해야한다.

# cmd에서 python을 치면 프롬프트영역으로 진행되서 바로 콘솔로그 같은 값이 나오게 된다. (브라우저 콘솔창과 비슷)  => 인터프리터 언어다 + 대신 프롬프트 영역에 기입하면 자료들이 저장이 안되므로 파이선 파일에 만들어 사용한다. + 나가려면 exit() 치면된다.

# 파이썬 독립적으로 프로젝트 관리 하고 싶다 (가상화로) = python -m venv(명령어- 바꿀수 없음) venv(폴더명- 바꿀 수 있음) => 만들어진 폴더에 만들면 개인화 된다.

# 만들어진 venv에서 scripts에 들어가서 venv\Scripts\activate 를 사용해서 venv파일 만든다

# 가상화를 하면 파일 설치를 한 것을 그냥 만들어진 비밀폴더 지우면 다 삭제된다.

# 쥬피터와 파이선의 차이 쥬피터는 작은 셀로 지역적으로 사용 가능한 느낌 == google colab과 비슷함(그리고 이건 외부 공유된 파이선 파일 실행할때 많이 사용함 - 마크다운도 가능 + 단행 실행 or 다 실행 가능 그리고 구글드라이브에도 저장이 가능 그래서 공유 가능 + 자신의 컴퓨터 사양이 안좋을 때 코랩 이용해서 구동 가능) vs 파이썬은 변수설정 전역변수 설정하는 느낌

# 파이썬 공유 ==> 코랩 사용 이건 쥬피터와 비슷한 형식이다

# 그리고 쥬피터 파일은 개인화한 상태에서 설치한 채로 내용물을 기입하면 가장 상위 폴더내에 내용물 저장가능 + 쥬피터 파일 깃허브로 올리면 프리뷰형식으로 쥬피터 화면 내용이 보인다. 실행만 안된다. 

# study01(쥬피터),02(파이선),03(uv를 이용한 fastapi)폴더에 사용법과 내용이 있으니 봐라

# 쥬피터 사용하는데 커널 누르고 파이썬으로 연결하면 사용가능 

# 파이썬에서는 자료형 찾기는 type() 이고 숫자 자료형은 int(정수) + float(실수) , 문자 자료형은 str, 그리고 문자와 숫자는 더할 수 없고 곱하기만 가능하다. 그리고 불린형이 있는데 여기서 true의 경우 자바스크립트는 소문자지만 파이썬은 첫글자 대문자 여야한다. 

# 코딩스타일에서 변수 함수표기법 3가지 1카멜 2파스칼 3스네이크 기억하기 데이터베이스에서는 스네이크 많이 쓴다

# 쥬피터 영역에서는 print없이 변수만 사용하여도 변수의 값이 출력된다.

# 자바스크립트에서는 자료형 비교 === 으로 사용가능하지만 파이선에서는 각 변수에 타입함수호출해서 ==으로 비교하면된다.

# 줄바꿈 사용하기 위해 ''' ''' 3개씩으로 나눠서 사용. 나중에 ai관련 플젝할때 프롬프트 사용위해 쓸 것이다. ''와 같이 다 문자열이 된다. ``도 사용가능한데 둘다 백틱효과 사용가능하다.\

# 자바스크립트에서는 undefined와 null등이 있는데 파이썬에서는 비슷한 내용으로 None 하나 있다. 이건 type이다

# 